# PDCurses

Vendored from https://github.com/wmcbrine/PDCurses tag `3.9` (public domain).
Only the core library, the Windows console (wincon) port, and the ACS tables
are kept; demos and other platform ports are dropped. Built by the top-level
Makefile with `PDC_WIDE` and `PDC_FORCE_UTF8`.
